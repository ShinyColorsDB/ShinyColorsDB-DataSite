export interface CD {
  albumIndex: number;
  albumId: string;
  albumTitle: string;
  albumName: string;
  albumArtist: string;
  artistDetail: string;
  albumCategory: string;
  albumReleaseDate: string;
}